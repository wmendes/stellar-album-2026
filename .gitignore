# Rust / Cargo build output
/target
**/target

# Soroban / Stellar CLI local state
.stellar/

# Built wasm artifacts (built on demand via `stellar contract build`)
*.wasm

# Soroban test snapshots (auto-generated, regenerated on each test run)
**/test_snapshots/

# OS / editor noise
.DS_Store
*.swp

# Note: Cargo.lock IS committed (contract builds should be reproducible).
